@model IEnumerable<FitGoals.Models.Measurement>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MeasurementName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MeasurementName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MeasurementID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MeasurementID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MeasurementID })
        </td>
    </tr>
}

</table>
